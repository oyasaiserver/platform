# BigWolf プラグイン - OP・管理者向けガイド

## 目次

1. [概要](#%E6%A6%82%E8%A6%81)
1. [基本設定](#%E5%9F%BA%E6%9C%AC%E8%A8%AD%E5%AE%9A)
1. [管理コマンド一覧](#%E7%AE%A1%E7%90%86%E3%82%B3%E3%83%9E%E3%83%B3%E3%83%89%E4%B8%80%E8%A6%A7)
1. [アイテム配布](#%E3%82%A2%E3%82%A4%E3%83%86%E3%83%A0%E9%85%8D%E5%B8%83)
1. [ショップシステム](#%E3%82%B7%E3%83%A7%E3%83%83%E3%83%97%E3%82%B7%E3%82%B9%E3%83%86%E3%83%A0)
1. [強制収納機能](#%E5%BC%B7%E5%88%B6%E5%8F%8E%E7%B4%8D%E6%A9%9F%E8%83%BD)
1. [設定ファイル](#%E8%A8%AD%E5%AE%9A%E3%83%95%E3%82%A1%E3%82%A4%E3%83%AB)
1. [トラブルシューティング](#%E3%83%88%E3%83%A9%E3%83%96%E3%83%AB%E3%82%B7%E3%83%A5%E3%83%BC%E3%83%86%E3%82%A3%E3%83%B3%E3%82%B0)
1. [権限管理](#%E6%A8%A9%E9%99%90%E7%AE%A1%E7%90%86)

______________________________________________________________________

## 概要

このガイドは、BigWolfプラグインを管理するOP（オペレーター）向けの包括的なマニュアルです。サーバー管理、プレイヤーサポート、トラブル対応に必要な全機能を網羅しています。

### 必要な権限

- **OP権限**: すべての管理コマンドにはOP権限が必要です
- **権限ノード**: `bigwolf.admin`（現在未使用、将来の拡張用）

______________________________________________________________________

## 基本設定

### 初回セットアップ

1. **プラグインのインストール**

   ```
   plugins/
   ├── OyasaiAnalytics.jar  (BigWolf含む)
   └── TokenManager.jar      (必須依存)
   ```

1. **サーバー起動** 初回起動時に自動で`config.yml`が生成されます。

1. **設定の確認**

   ```
   plugins/OyasaiAnalytics/config.yml
   ```

1. **設定のリロード**

   ```
   /bigwolf reload
   ```

### 依存プラグイン

#### 必須

- **TokenManager**: ポイントシステムに使用
  - プレイヤーへのポイント付与: `/tm add <player> <amount>`
  - ポイント確認: `/tm balance <player>`

#### 推奨

- **LuckPerms**: 詳細な権限管理
- **WorldGuard**: ペット召喚制限
- **CoreProtect**: ログ記録

______________________________________________________________________

## 管理コマンド一覧

### `/bigwolf reload`

設定ファイルをリロードします。

**使用例**:

```
/bigwolf reload
```

**用途**:

- config.ymlを編集した後
- ペットのステータス設定を変更した後
- 価格設定を変更した後

**注意**: プラグイン全体の再起動ではなく、設定のみリロード

______________________________________________________________________

### `/bigwolf item <種類>`

OP専用アイテムを配布します。

#### サブコマンド一覧

##### `/bigwolf item food`

魔法のペットフードを入手（レベルアップ用）

##### `/bigwolf item brush`

ペットブラシを入手（愛情表現用）

##### `/bigwolf item treat`

ペットのおやつを入手（体力回復用）

##### `/bigwolf item toys`

全種類のおもちゃセットを入手

##### `/bigwolf item skillbook`

全スキルブック（Lv.1〜3）を入手

##### `/bigwolf item skillbook1`

スキルブックLv.1（咆哮の書）のみ

##### `/bigwolf item skillbook2`

スキルブックLv.2（突進の書）のみ

##### `/bigwolf item skillbook3`

スキルブックLv.3（極意の書）のみ

##### `/bigwolf item all`

全アイテム（フード、ブラシ、おやつ、おもちゃ、スキルブック）を入手

**使用例**:

```
/bigwolf item food
/bigwolf item all
```

**用途**:

- イベント景品の配布
- プレイヤーへの報酬
- テスト・デバッグ

______________________________________________________________________

### 個別アイテム配布（旧コマンド、互換性維持）

#### `/bigwolf food`

魔法のペットフードを配布

```
/bigwolf food
```

#### `/bigwolf toy`

おもちゃを配布（現在は`item toys`を推奨）

#### `/bigwolf skillbook`

全スキルブックを配布

```
/bigwolf skillbook
```

**注意**: これらのコマンドは`/bigwolf item`に統合されましたが、後方互換性のため使用可能です。

______________________________________________________________________

### `/bigwolf exp <mob名> [variant]`

実験的にペットを直接召喚します（ポイント消費なし）。

**使用例**:

```
/bigwolf exp wolf
/bigwolf exp cat siamese
/bigwolf exp parrot blue
```

**用途**:

- テスト・デバッグ
- イベント用ペット配布
- 特別報酬

**注意**:

- 購入券は発行されません
- 直接プレイヤーの位置に召喚されます
- 3匹制限は適用されます

______________________________________________________________________

## ショップシステム

ワールド内に購入用ショップMOBを設置できます。プレイヤーはこのMOBをクリックして購入できます。

### `/bigwolf shop <mob名> [variant] [cost] [yaw]`

ショップMOBを設置します。

**パラメータ**:

- `<mob名>`: 販売するペットの種類（必須）
- `[variant]`: バリアント（オプション）
- `[cost]`: 価格（オプション、デフォルト: 100）
- `[yaw]`: 向き（度数、オプション、省略時は実行者の向き）

**使用例**:

```
/bigwolf shop wolf
/bigwolf shop cat tabby 150
/bigwolf shop parrot red 200 90
```

**ショップMOBの特徴**:

- ✅ **無敵**: ダメージを受けない
- ✅ **AI無効**: 動かない、攻撃しない
- ✅ **持続性**: サーバー再起動後も残る
- ✅ **クリック購入**: プレイヤーがクリックで購入画面を開ける

**設置手順**:

1. 設置したい場所に立つ
1. ショップMOBを向けたい方向を向く
1. コマンドを実行
1. ショップMOBが足元に出現

### `/bigwolf shopremove`

視線の先のショップMOBを削除します。

**使用方法**:

1. 削除したいショップMOBを見る（15ブロック以内）
1. コマンドを実行

**使用例**:

```
/bigwolf shopremove
```

### `/bigwolf shopremoveall`

全ワールドの全ショップMOBを削除します（確認必須）。

**使用方法**:

1. 1回目の実行: 確認メッセージが表示される
1. 15秒以内に再度実行: 全削除が実行される
1. 15秒経過: 確認がリセットされる

**使用例**:

```
/bigwolf shopremoveall  （確認）
/bigwolf shopremoveall  （実行）
```

**⚠️ 警告**:

- この操作は取り消せません
- 全ワールドの全ショップMOBが削除されます
- 慎重に使用してください

______________________________________________________________________

## 強制収納機能

他のプレイヤーのペットを強制的に収納できます。

### `/bigwolf force_store`

視線の先のペットを強制収納します。

**使用方法**:

1. 収納したいペットを見る（15ブロック以内）
1. コマンドを実行
1. ペットがスポーンエッグ化され、所有者のインベントリに送られる

**使用例**:

```
/bigwolf force_store
```

**メッセージ**:

```
対象のペットを強制収納しました。(Owner: PlayerName)
```

**用途**:

- 放置されたペットの整理
- トラブル対応
- ワールド編集前のペット退避

### `/bigwolf force_storeall <player>`

指定プレイヤーの全ペットを強制収納します。

**使用例**:

```
/bigwolf force_storeall PlayerName
```

**メッセージ**:

```
PlayerName のペット 3匹 を強制収納しました。
```

**用途**:

- プレイヤーのペットを一括管理
- 緊急時の対応
- サーバーメンテナンス前の整理

______________________________________________________________________

## 設定ファイル

### config.yml の構造

```yaml
economy:
  foodPointCost: 100 # フードのコスト

pets:
  maxFoodLevel: 50 # 最大レベル
  wolf: # ペット種別ごとの設定
    baseSpeed: 0.6 # 初期速度
    maxSpeed: 1.2 # 最大速度
    jumpPower: 0.6 # ジャンプ力
    scaleMin: 1.5 # 初期サイズ
    scaleMax: 2.5 # 最大サイズ
  # ... 他のペット設定

shop:
  defaultCost: 100 # デフォルト購入価格

skillbook:
  costLv1: 50 # スキルブックLv.1コスト
  costLv2: 100 # スキルブックLv.2コスト
  costLv3: 150 # スキルブックLv.3コスト
```

### 設定のカスタマイズ

#### 価格設定の変更

```yaml
shop:
  defaultCost: 200 # 購入価格を200ptに

economy:
  foodPointCost: 50 # レベルアップを50ptに
```

#### ペット性能の調整

```yaml
pets:
  wolf:
    baseSpeed: 0.8 # 速度を上げる
    maxSpeed: 1.5
    jumpPower: 0.8 # ジャンプ力を上げる
    scaleMin: 2.0 # サイズを大きく
    scaleMax: 3.0
```

#### スキルブックの価格調整

```yaml
skillbook:
  costLv1: 100 # 各レベルの価格を変更
  costLv2: 200
  costLv3: 300
```

### 設定反映

```
/bigwolf reload
```

______________________________________________________________________

## トラブルシューティング

### 問題1: ペットが消えた

**原因**: サーバークラッシュ、再起動 **解決策**:

1. プレイヤーに`/bigwolf storeall`を事前に使用してもらう
1. 定期的なバックアップ推奨

### 問題2: ポイントが減らない

**原因**: TokenManagerとの連携エラー **確認**:

```
/tm balance <player>
```

**解決策**:

1. TokenManagerが正常に動作しているか確認
1. サーバーログを確認
1. 両プラグインを最新版に更新

### 問題3: ペットが召喚できない

**原因**: 保護プラグインによる制限 **確認**:

- WorldGuardの`mob-spawning`フラグ
- 他の保護プラグインの設定

**解決策**: 該当エリアでペット召喚を許可

### 問題4: ショップMOBが消える

**原因**: 他プラグインによるMOB削除 **解決策**:

1. MobClearなどのプラグインの除外設定
1. エンティティ管理プラグインの設定確認

### 問題5: 設定が反映されない

**解決策**:

```
/bigwolf reload
```

または

```
/reload confirm  （全プラグインリロード）
```

### 問題6: プレイヤーのペットが多すぎる

**解決策**:

```
/bigwolf force_storeall <player>
```

______________________________________________________________________

## 権限管理

### 現在の権限システム

- **OP**: すべての管理コマンド使用可能
- **非OP**: プレイヤー向けコマンドのみ

### 将来的な権限ノード（予定）

```yaml
permissions:
  bigwolf.admin: # 全管理コマンド
    default: op
  bigwolf.use: # 基本使用
    default: true
  bigwolf.shop: # ショップ管理
    default: op
  bigwolf.force: # 強制収納
    default: op
  bigwolf.item: # アイテム配布
    default: op
```

______________________________________________________________________

## 運用ベストプラクティス

### 1. 定期メンテナンス

```
# 毎週実施推奨
1. 放置ペットの確認と整理
2. ショップMOBの状態確認
3. プレイヤーからのフィードバック収集
```

### 2. イベント運営

```
# ペット配布イベント
1. /bigwolf item all で景品セット作成
2. 特定のペット購入券を配布
3. レア度の高いバリアントを景品に
```

### 3. 経済バランス

```
# 価格調整の目安
- 初心者向け: 50-100pt
- 標準: 100-200pt
- レア: 300-500pt
- 超レア: 1000pt+
```

### 4. プレイヤーサポート

```
# よくある問い合わせ対応
1. ポイント不足 → /tm add で支援
2. ペット消失 → force_store で整理後、補償
3. 操作不明 → プレイヤーガイドを案内
```

### 5. パフォーマンス管理

```
# サーバー負荷が高い場合
1. MAX_PET_COUNT を 2 に減らす
2. ペット総数を定期的にチェック
3. 不要なショップMOBを削除
```

______________________________________________________________________

## デバッグ・ログ

### ログ確認

```
logs/latest.log
```

### 重要なログメッセージ

- `BigWolfPlugin enabled`: 起動成功
- `BigWolf: config reloaded!`: 設定リロード成功
- TokenManagerコマンド実行ログ

### トラブル時の情報収集

1. サーバーバージョン
1. プラグインバージョン
1. エラーログ
1. 再現手順

______________________________________________________________________

## コマンド一覧表

| コマンド | 説明 | OP専用 | |---------|------|-------| | `/bigwolf` | 使用方法表示 | ❌ | |
`/bigwolf <mob>` | ペット購入 | ❌ | | `/bigwolf storeall` | 全ペット収納 | ❌ | |
`/bigwolf reload` | 設定リロード | ✅ | | `/bigwolf item <type>` | アイテム配布 | ✅ | |
`/bigwolf food` | フード配布 | ✅ | | `/bigwolf toy` | おもちゃ配布 | ✅ | |
`/bigwolf skillbook` | スキルブック配布 | ✅ | | `/bigwolf exp <mob>` | 実験的召喚 | ✅ | |
`/bigwolf shop <mob>` | ショップ設置 | ✅ | | `/bigwolf shopremove` | ショップ削除 | ✅ | |
`/bigwolf shopremoveall` | 全ショップ削除 | ✅ | | `/bigwolf force_store` | 強制収納 | ✅ | |
`/bigwolf force_storeall <player>` | プレイヤーペット収納 | ✅ |

______________________________________________________________________

## サポート・フィードバック

プラグインの改善提案やバグレポートは、開発者またはサーバー管理者にご連絡ください。

______________________________________________________________________

**最終更新**: 2026-01-11\
**対応バージョン**: BigWolf 1.0\
**対象**: OP・管理者
