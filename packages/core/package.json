{
  "name": "@oyasaiserver/core",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "start": "dist/index.js"
  },
  "files": [
    "dist",
    "overlays",
    "compose.yaml"
  ],
  "scripts": {
    "build": "tsup",
    "deploy": "node scripts/deploy.ts",
    "prestart": "npm run build",
    "start": "cp dist/index.js server/index.js && cd server && node ."
  },
  "dependencies": {
    "decompress": "^4.2.1",
    "webpod": "^1.0.0"
  },
  "devDependencies": {
    "@types/decompress": "^4.2.7",
    "tsup": "^8.5.0"
  }
}
