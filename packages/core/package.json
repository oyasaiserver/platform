{
  "name": "@oyasaiserver/core",
  "version": "1.0.0",
  "type": "module",
  "bin": "dist/bin",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "deploy": "tsx scripts/deploy.ts",
    "prestart": "npm run build && npm run deploy",
    "start": "cd server && ./local/bin"
  },
  "config": {
    "sea": "sea-config.json"
  },
  "dependencies": {
    "decompress": "^4.2.1",
    "webpod": "^1.0.0"
  },
  "devDependencies": {
    "@types/decompress": "^4.2.7",
    "postject": "^1.0.0-alpha.6",
    "tsdown": "^0.12.9"
  }
}
