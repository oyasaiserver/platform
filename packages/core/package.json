{
  "name": "@oyasaiserver/core",
  "version": "1.0.0",
  "type": "module",
  "files": [
    "dist",
    "overlays",
    "compose.yaml"
  ],
  "scripts": {
    "build": "tsup",
    "deploy": "tsx scripts/deploy.ts",
    "prestart": "npm run build",
    "start": "cp dist/* server && cd server && node ."
  },
  "dependencies": {
    "decompress": "^4.2.1",
    "webpod": "^1.0.0"
  },
  "devDependencies": {
    "@types/decompress": "^4.2.7",
    "tsup": "^8.5.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
