{
  "name": "@oyasaiserver/core",
  "version": "1.0.0",
  "type": "module",
  "bin": "dist/core",
  "scripts": {
    "build": "node scripts/build.ts",
    "build-and-deploy": "npm run build && npm run deploy",
    "deploy": "node scripts/deploy.ts",
    "prestart": "npm run build-and-deploy",
    "start": "cd server/local && ./core",
    "pretest": "npm run build-and-deploy",
    "test": "node --test"
  },
  "config": {
    "sea": "sea-config.json"
  },
  "dependencies": {
    "decompress": "^4.2.1"
  },
  "devDependencies": {
    "@types/decompress": "^4.2.7",
    "postject": "^1.0.0-alpha.6",
    "tsdown": "^0.12.9"
  }
}
