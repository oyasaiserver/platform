{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Node.js SEA Configuration",
  "type": "object",
  "required": ["main", "output"],
  "properties": {
    "main": {
      "type": "string",
      "description": "Absolute path to the bundled main JavaScript file."
    },
    "output": {
      "type": "string",
      "description": "Absolute path where the generated .blob file will be written."
    },
    "disableExperimentalSEAWarning": {
      "type": "boolean",
      "default": false,
      "description": "Disables the experimental SEA warning if true."
    },
    "useSnapshot": {
      "type": "boolean",
      "default": false,
      "description": "If true, enables V8 snapshotting."
    },
    "useCodeCache": {
      "type": "boolean",
      "default": false,
      "description": "If true, enables V8 code caching."
    },
    "assets": {
      "type": "object",
      "description": "Optional map of asset keys to absolute paths.",
      "additionalProperties": {
        "type": "string"
      }
    }
  },
  "additionalProperties": false
}
