{
  "type": "module",
  "workspaces": [
    "apps/*",
    "gen/json/ts",
    "gen/proto/ts",
    "infra/*",
    "lib/ts",
    "packages/*",
    "schema/ts"
  ],
  "scripts": {
    "build": "tsc --build",
    "bump": "taze",
    "codegen": "codegen && npm run format && ./gradlew spotlessApply",
    "format": "sort-package-json '**/package.json' && buf format --write && prettier . --write",
    "lint": "oxlint --fix"
  },
  "dependencies": {
    "semver": "^7.7.2",
    "zod": "^4.0.5",
    "zx": "^8.7.1"
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.55.1",
    "@prettier/plugin-oxc": "^0.0.4",
    "@types/node": "^24.0.15",
    "oxlint": "^1.7.0",
    "prettier": "^3.6.2",
    "sort-package-json": "^3.4.0",
    "taze": "^19.1.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.25.0"
  }
}
