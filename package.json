{
  "type": "module",
  "workspaces": [
    "**"
  ],
  "scripts": {
    "build": "tsc --build",
    "bump": "ncu --deep --upgrade --reject @oyasaiserver/* --install always",
    "check": "biome check --write && sort-package-json '**/package.json' --quiet",
    "codegen": "codegen"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.5",
    "@types/node": "^24.0.4",
    "npm-check-updates": "^18.0.1",
    "sort-package-json": "^3.3.1",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "wrangler": "^4.21.2"
  }
}
