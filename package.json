{
  "type": "module",
  "workspaces": [
    "apps/*",
    "bin",
    "gen/json/ts",
    "gen/proto/ts",
    "infra/*",
    "lib/ts",
    "packages/*",
    "schema/ts"
  ],
  "scripts": {
    "build": "tsc --build",
    "bump": "taze",
    "check": "sort-package-json '**/package.json' --quiet && buf format --write && gradle spotlessApply",
    "codegen": "codegen && npm run format && gradle spotlessApply",
    "lint": "oxlint --fix",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@bufbuild/buf": "^1.55.1",
    "@prettier/plugin-oxc": "^0.0.4",
    "@types/node": "^24.0.12",
    "oxlint": "^1.6.0",
    "prettier": "^3.6.2",
    "sort-package-json": "^3.4.0",
    "taze": "^19.1.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.24.0",
    "zod": "^3.25.76",
    "zx": "^8.6.2"
  }
}
