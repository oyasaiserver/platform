# Data source used to initialize the in-memory SocialLikes build cache.
# YAML is safe by default. sqlite is honoured only after /sldataop migrate apply records the
# completion marker; otherwise the plugin warns and continues from YAML.
readSource: yaml

# Interval in minutes for periodic dirty build reconciliation (SQLite retry).
reconcileIntervalMinutes: 10

# ID migration is never automatic at startup. With player access stopped, use the console:
# /sldataop migrate dry-run
# /sldataop migrate apply
