name: Deploy Platform

on:
  workflow_call:
    inputs:
      environment:
        required: true
        type: string

permissions:
  id-token: write

jobs:
  infra:
    secrets: inherit
    uses: ./.github/workflows/deploy-platform-stack.yaml
    with:
      environment: ${{ inputs.environment }}
      stack: infra

  services:
    needs:
      - infra
    secrets: inherit
    uses: ./.github/workflows/deploy-platform-stack.yaml
    with:
      environment: ${{ inputs.environment }}
      stack: services
