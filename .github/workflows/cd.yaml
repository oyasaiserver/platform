name: CD

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  deploy:
    secrets: inherit
    permissions:
      id-token: write
    uses: ./.github/workflows/deploy-platform.yaml
    with:
      environment: master
