name: CD

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      environment:
        description: '⚠️ Deploy to production ⚠️'
        required: true
        default: production
        type: choice
        options:
          - production
          - development

jobs:
  envs:
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.environment.outputs.ENVIRONMENT }}
    steps:
      - id: environment
        # language=bash
        run: |
          ENVIRONMENT="development"
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            ENVIRONMENT="${{ github.event.inputs.environment }}"
          fi
          echo "ENVIRONMENT=$ENVIRONMENT" >> "$GITHUB_OUTPUT"

  deploy-apps:
    needs:
      - envs
    runs-on: ubuntu-latest
    environment: ${{ needs.envs.outputs.ENVIRONMENT }}
    strategy:
      matrix:
        app: [wiki]
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
      - run: |
          npm run build --if-present
          npm run deploy -- --name ${{ matrix.app }}-${{ needs.envs.outputs.ENVIRONMENT }} > /dev/null
        working-directory: apps/${{ matrix.app }}
        env:
          ENVIRONMENT: ${{ needs.envs.outputs.ENVIRONMENT }}
          DOTENV_PRIVATE_KEY: ${{ secrets.DOTENV_PRIVATE_KEY }}

  cdktf:
    needs:
      - envs
      - deploy-apps
    runs-on: ubuntu-latest
    environment: ${{ needs.envs.outputs.ENVIRONMENT }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-node
      - uses: hashicorp/setup-terraform@v3
      - run: |
          npm run build
          npm run deploy
        working-directory: infra/cdktf
        env:
          ENVIRONMENT: ${{ needs.envs.outputs.ENVIRONMENT }}
          DOTENV_PRIVATE_KEY: ${{ secrets.DOTENV_PRIVATE_KEY }}

  deploy-onprem:
    needs:
      - envs
      - cdktf
    if: needs.envs.outputs.ENVIRONMENT == 'production'
    runs-on: ubuntu-latest
    environment: ${{ needs.envs.outputs.ENVIRONMENT }}
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/setup-java
      - uses: ./.github/actions/setup-node
      - run: ./gradlew build --parallel
      - run: npm run deploy
        working-directory: infra/onprem
        env:
          ENVIRONMENT: ${{ needs.envs.outputs.ENVIRONMENT }}
          DOTENV_PRIVATE_KEY: ${{ secrets.DOTENV_PRIVATE_KEY }}
