{
  "name": "@infra/local",
  "type": "module",
  "scripts": {
    "build": "bun build --compile --minify src/index.ts --outfile dist/platform-server",
    "deploy": "npm run build && tsx src/deploy.ts"
  },
  "dependencies": {
    "bun": "^1.2.14"
  },
  "devDependencies": {
    "@types/bun": "^1.2.14"
  }
}
